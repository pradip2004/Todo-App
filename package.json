{
  "name": "todo-project",
  "version": "1.0.0",
  "description": "",
  
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm install) || (cd todo && npm install)",
    "server": "node ./server/server.js",
    "client": "cd todo && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "if-env": "^1.0.4",
    "pg": "^8.12.0"
  }
}
